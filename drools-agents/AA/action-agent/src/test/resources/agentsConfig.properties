agent.endpoint.ip=${agent.endpoint.ip}
agent.endpoint.port=${agent.endpoint.port}
agent.name=${agent.name}
agent.endpoint.url=http://${agent.endpoint.ip}:${agent.endpoint.port}/${agent.name}/services/AsyncAgentService?wsdl